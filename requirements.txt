# Misaka MediaFlow Python Dependencies

# ---- Web framework ----
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# ---- Database ----
sqlalchemy>=2.0.0
alembic>=1.13.0
# aiosqlite 已移除: 项目使用 MySQL/PostgreSQL，不再使用 SQLite
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
pymysql>=1.1.0
aiomysql>=0.2.0

# ---- HTTP / Cache ----
httpx>=0.27.0
redis>=5.0.0

# ---- Scheduler / Config ----
apscheduler>=3.10.0
pyyaml>=6.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ---- CloudDrive2 gRPC client ----
clouddrive2-client>=0.1.0

# ---- 115 drive ----
p115client

# ---- Utils ----
python-multipart>=0.0.9

# ---- Subtitle font subsetting (builtin engine) ----
fonttools>=4.51.0         # font subsetting: pyftsubset
# brotli 为可选项: fonttools woff2 压缩支持，不影响核心子集化功能
# brotli>=1.1.0
