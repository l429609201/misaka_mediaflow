# Misaka MediaFlow Python Dependencies

# ---- Web 框架 ----
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# ---- 数据库 ----
sqlalchemy>=2.0.0
alembic>=1.13.0
aiosqlite>=0.20.0               # SQLite 异步驱动（默认数据库）
asyncpg>=0.29.0                 # PostgreSQL 异步驱动
psycopg2-binary>=2.9.9          # PostgreSQL 同步驱动（兼容层 / 同步引擎）
pymysql>=1.1.0                  # MySQL 同步驱动 (建库用)
aiomysql>=0.2.0                 # MySQL 异步驱动

# ---- HTTP / 缓存 ----
httpx>=0.27.0
redis>=5.0.0

# ---- 调度 / 配置 ----
apscheduler>=3.10.0
pyyaml>=6.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ---- CloudDrive2 gRPC 客户端 ----
clouddrive2-client>=0.1.0       # CloudDrive2 存储适配器（gRPC）

# ---- 115 网盘 ----
# p115client 使用纯 httpx，无额外依赖（已通过 httpx 覆盖）
# p115rsacipher 要求 Python>=3.12，镜像是 3.11，改为内嵌实现（见 p115_rsa.py）

# ---- 工具 ----
python-multipart>=0.0.9         # FastAPI 文件上传 / Form 解析
